export type CategoryType = {
  name: string;
  imageurl: string;
};

export type businessType = {
  name: string;
  imageurl: string;
  address: string;
  rating: number;
  category: string; // Random category added
};
